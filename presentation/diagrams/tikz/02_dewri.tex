\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.7cm, minimum height=0.58cm, align=center,
                line width=0.8pt, font=\scriptsize},
    step/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=4.4cm, minimum height=0.55cm, align=center,
                line width=0.8pt, font=\scriptsize},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=7pt, line width=1.2pt},
    arr/.style={-{Latex[length=1.8mm]}, line width=0.8pt, draw=gray!60},
    lbl/.style={font=\scriptsize\bfseries}
]

% Orchestrator
\node[box=ugmblue] (orch) {EscalationOrchestratorTool\\5 steps \textbullet{} internal};
\node[group=ugmblue, fit=(orch), label={[lbl]above:Escalation Agent}] (esc) {};

\node[box=degreen, right=2.2cm of orch] (sync) {GitHubDbIssueSyncTool};
\node[group=degreen, fit=(sync), label={[lbl]above:GitHub DB Sync Agent}] (syncdb) {};

% Pipeline steps
\node[step=degreen, below=1.1cm of orch, xshift=0cm] (s1) {1 \textbullet{} Fetch issues};
\node[step=degreen, below=0.3cm of s1] (s2) {2 \textbullet{} Detect recurring};
\node[step=degreen, below=0.3cm of s2] (s3) {3 \textbullet{} Lookup GitHub};
\node[step=degreen, below=0.3cm of s3] (s4) {4 \textbullet{} Comment / Create / Close};
\node[step=degreen, below=0.3cm of s4] (s5) {5 \textbullet{} Send email};
\node[group=degreen, fit=(s1)(s2)(s3)(s4)(s5),
      label={[lbl]below:Pipeline}] (pipe) {};

% Data sources
\node[box=dered, right=2.0cm of s2] (db) {issues \textbullet{} reports\\PostgreSQL};
\node[box=dered, below=0.4cm of db] (gh) {GitHub API};
\node[box=dered, below=0.4cm of gh] (em) {Email};

\draw[arr] (orch.south) -- (s1.north);
\draw[arr] (s1.south) -- (s2.north);
\draw[arr] (s2.south) -- (s3.north);
\draw[arr] (s3.south) -- (s4.north);
\draw[arr] (s4.south) -- (s5.north);
\draw[arr] (s2.east) -- (db.west);
\draw[arr] (s3.east) -- (gh.west);
\draw[arr] (s5.east) -- (em.west);
\draw[arr] (sync.west) -- (orch.east);

\end{tikzpicture}
\end{document}
