\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.45cm and 0.35cm]
    \node[appbox] (trigger) {AIP schedule or\\manual trigger};
    \node[processbox, right=0.65cm of trigger] (coordinator) {Issue Reoccurrence\\coordinator};
    \node[groupbox=ugmblue, fit=(trigger)(coordinator),
        label={[grouplabel]above:Weekly run}] (run) {};

    \node[processbox, below=1.0cm of coordinator, xshift=-5.2cm] (fetch) {1. Fetch employee\\issues};
    \node[processbox, right=0.25cm of fetch] (detect) {2. Detect\\recurrence};
    \node[processbox, right=0.25cm of detect] (lookup) {3. Lookup GitHub\\account + repo};
    \node[processbox, right=0.25cm of lookup] (change) {4. Search, comment,\\create, or close};
    \node[processbox, right=0.25cm of change] (notify) {5. Send email\\and summary};
    \node[groupbox=diagramgreen, fit=(fetch)(detect)(lookup)(change)(notify),
        label={[grouplabel]below:EscalationOrchestratorTool}] (pipeline) {};

    \node[externalbox, below=0.9cm of detect] (db) {PostgreSQL\\issues + reports};
    \node[externalbox, below=0.9cm of change] (github) {GitHub API\\issue state};
    \node[externalbox, below=0.9cm of notify] (email) {Email\\employee notification};

    \draw[arrow] (run.south) -- (fetch.north);
    \draw[arrow] (fetch) -- (detect);
    \draw[arrow] (detect) -- (lookup);
    \draw[arrow] (lookup) -- (change);
    \draw[arrow] (change) -- (notify);
    \draw[arrow] (fetch.south) |- (db.north);
    \draw[arrow] (change.south) -- (github.north);
    \draw[arrow] (notify.south) -- (email.north);
\end{tikzpicture}
\end{document}
