\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.35cm and 0.55cm]
    \node[appbox] (coord) {WeeklyReportDigitalEmployee\\AIP coordinator};

    \node[processbox, below=0.9cm of coord, xshift=-3.6cm] (ingest) {weekly report\\ingestor agent};
    \node[processbox, right=0.25cm of ingest] (qna) {weekly report\\Q\&A agent};
    \node[processbox, right=0.25cm of qna] (update) {report updater\\agent};
    \node[processbox, right=0.25cm of update] (sender) {report email\\sender agent};
    \node[processbox, right=0.25cm of sender] (e2b) {E2B issue\\processing agent};
    \node[groupbox=diagramgreen, fit=(ingest)(qna)(update)(sender)(e2b),
        label={[grouplabel]below:Sub-agents}] (subagents) {};

    \node[externalbox, below=0.95cm of qna] (sql) {GL Connector SQL\\custom tool};
    \node[externalbox, right=0.35cm of sql] (drive) {Google Docs /\\Drive / Sheets};
    \node[externalbox, right=0.35cm of drive] (pg) {PostgreSQL\\reports + issues};
    \node[groupbox=diagramred, fit=(sql)(drive)(pg),
        label={[grouplabel]below:Data and external services}] (data) {};

    \draw[arrow] (coord.south) -- (subagents.north);
    \draw[arrow] (ingest.south) -- (sql.north);
    \draw[arrow] (qna.south) -- (sql.north);
    \draw[arrow] (update.south) -- (pg.north);
    \draw[arrow] (sender.south) -- (drive.north);
    \draw[arrow] (e2b.south) -- (drive.north);
\end{tikzpicture}
\end{document}
