\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.45cm and 0.55cm]
    \node[processbox] (unit) {Unit tests\\models + tools};
    \node[processbox, below=of unit] (security) {Security regression\\SQL identifier validation};
    \node[processbox, below=of security] (integration) {Integration tests\\real database + MCP fixtures};
    \node[groupbox=diagramgreen, fit=(unit)(security)(integration),
        label={[grouplabel]above:Pytest quality gates}] (pytest) {};

    \node[appbox, right=1.1cm of unit] (mom) {Playwright E2E\\MoM flow};
    \node[appbox, below=of mom] (datasaur) {Playwright E2E\\Datasaur flow};
    \node[appbox, below=of datasaur] (env) {Env-driven\\single-agent pattern};
    \node[groupbox=ugmblue, fit=(mom)(datasaur)(env),
        label={[grouplabel]above:Integration system tests}] (e2e) {};

    \node[externalbox, right=1.1cm of mom] (result) {Coverage, logs, and\\validated deployment};

    \draw[arrow] (pytest.east) -- (e2e.west);
    \draw[arrow] (e2e.east) -- (result.west);
\end{tikzpicture}
\end{document}
