\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.9cm, minimum height=0.55cm, align=center,
                line width=0.8pt, font=\scriptsize},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=1.8mm]}, line width=0.8pt, draw=gray!60},
    lbl/.style={font=\scriptsize\bfseries}
]

% App / Gateway
\node[box=ugmblue] (app) {app};
\node[box=ugmblue, below=0.4cm of app] (gw) {IAMGateway\\create\_user\_with\_password};

% Provider
\node[box=degreen, right=2.4cm of gw] (prov) {PostgreSQLUserStoreMixin};
\node[box=degreen, below=0.35cm of prov] (tx) {one transaction};

% Rows
\node[box=dered, below=1.0cm of prov] (r1) {user row};
\node[box=dered, below=0.3cm of r1] (r2) {local\_credentials};
\node[box=dered, below=0.3cm of r2] (r3) {external\_identity};
\node[box=dered, below=0.3cm of r3] (r4) {roles};
\node[group=dered, fit=(r1)(r2)(r3)(r4),
      label={[lbl]below:atomic \textbullet{} no orphan user}] (rows) {};

\draw[arr] (app.south) -- (gw.north);
\draw[arr] (gw.east) -- (prov.west);
\draw[arr] (prov.south) -- (r1.north);
\draw[arr] (tx.south) -- (rows.north);

\end{tikzpicture}
\end{document}
