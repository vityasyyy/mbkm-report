\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.9cm, minimum height=0.72cm, align=center,
                line width=0.9pt, font=\large},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=2mm]}, line width=0.9pt, draw=gray!60},
    lbl/.style={font=\large\bfseries}
]

% Top: app -> gateway -> provider
\node[box=ugmblue] (app) {app};
\node[box=ugmblue, right=1.0cm of app] (gw) {IAMGateway\\create\_user\_with\_password};
\node[box=degreen, right=1.0cm of gw] (prov) {PostgreSQLUserStoreMixin\\one transaction};
\node[group=degreen, fit=(prov), label={[lbl]above:Provider level}] (provgrp) {};

% Bottom: rows
\node[box=dered, below=1.0cm of gw] (r1) {user row};
\node[box=dered, right=0.3cm of r1] (r2) {local\_credentials};
\node[box=dered, right=0.3cm of r2] (r3) {external\_identity};
\node[box=dered, right=0.3cm of r3] (r4) {roles};
\node[group=dered, fit=(r1)(r2)(r3)(r4),
      label={[lbl]below:atomic \textbullet{} no orphan user}] (rows) {};

\draw[arr] (app.east) -- (gw.west);
\draw[arr] (gw.east) -- (prov.west);
\draw[arr] (gw.south) -- (r1.north);
\draw[arr] (prov.south) -- (rows.north);

\end{tikzpicture}
\end{document}
