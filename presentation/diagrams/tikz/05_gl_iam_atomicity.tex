\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.5cm]
    \node[appbox] (caller) {Application\\caller};
    \node[appbox, right=of caller] (gateway) {IAMGateway\\delegates request};
    \node[processbox, right=of gateway] (provider) {PostgreSQLUserStoreMixin\\provider layer};
    \node[processbox, right=of provider] (session) {One DB session\\one transaction};

    \node[smallbox=diagramred, below=1.0cm of session, xshift=-3.9cm] (user) {users row};
    \node[smallbox=diagramred, right=0.25cm of user] (credential) {local\_credentials};
    \node[smallbox=diagramred, right=0.25cm of credential] (identity) {external\_identity};
    \node[smallbox=diagramred, right=0.25cm of identity] (roles) {roles};
    \node[groupbox=diagramred, fit=(user)(credential)(identity)(roles),
        label={[grouplabel]below:Atomic writes}] (writes) {};

    \node[processbox, below=0.8cm of session] (commit) {single COMMIT\\all writes succeed};
    \node[externalbox, right=0.55cm of commit] (rollback) {ROLLBACK\\no orphan user};

    \draw[arrow] (caller) -- (gateway);
    \draw[arrow] (gateway) -- (provider);
    \draw[arrow] (provider) -- (session);
    \draw[arrow] (session.south) -- (writes.north);
    \draw[arrow] (writes.south) -- (commit.north);
    \draw[arrow] (writes.east) -| (rollback.north);
\end{tikzpicture}
\end{document}
