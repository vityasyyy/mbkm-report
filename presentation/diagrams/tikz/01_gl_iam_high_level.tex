\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.3cm and 0.9cm]
    \node[appbox] (fastapi) {FastAPI dependencies\\get\_current\_user};
    \node[appbox, below=of fastapi] (django) {Django + DRF\\authentication backends};
    \node[appbox, below=of django] (cli) {GL-IAM CLI};
    \node[groupbox=ugmblue, fit=(fastapi)(django)(cli),
        label={[grouplabel]above:Consumers}] (consumers) {};

    \node[processbox, right=1.3cm of fastapi] (gateway) {IAMGateway\\central orchestrator};
    \node[processbox, below=of gateway] (auth) {AuthenticationProvider};
    \node[processbox, below=of auth] (session) {SessionProvider};
    \node[processbox, below=of session] (users) {UserStoreProvider\\RBAC};
    \node[processbox, below=of users] (keys) {ApiKeyProvider\\three-tier keys};
    \node[processbox, below=of keys] (agents) {AgentProvider\\delegation auth};
    \node[groupbox=diagramgreen, fit=(gateway)(auth)(session)(users)(keys)(agents),
        label={[grouplabel]above:Protocol interfaces}] (protocols) {};

    \node[externalbox, right=1.3cm of gateway] (postgres) {PostgreSQL\\SQLAlchemy provider};
    \node[externalbox, below=0.55cm of postgres] (keycloak) {Keycloak};
    \node[externalbox, below=0.55cm of keycloak] (stackauth) {StackAuth};
    \node[groupbox=diagramred, fit=(postgres)(keycloak)(stackauth),
        label={[grouplabel]above:Provider implementations}] (providers) {};

    \draw[arrow] (consumers.east) -- (protocols.west);
    \draw[arrow] (protocols.east) -- (providers.west);
\end{tikzpicture}
\end{document}
