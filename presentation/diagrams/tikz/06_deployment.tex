\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.65cm]
    \node[appbox] (pr) {Pull request\\review gate};
    \node[processbox, right=of pr] (actions) {GitHub Actions\\CI/CD pipeline};
    \node[processbox, right=of actions] (helm) {Helm chart\\ConfigMap + Secret};
    \node[appbox, right=of helm] (dev) {Development\\environment};
    \node[appbox, right=of dev] (prod) {Production\\environment};
    \node[groupbox=diagramgreen, fit=(actions)(helm),
        label={[grouplabel]above:Build and deploy}] (cicd) {};

    \node[processbox, below=0.9cm of prod] (cron) {Kubernetes CronJob\\scheduled agent run};
    \node[externalbox, below=0.55cm of cron] (logs) {Validate status\\and inspect logs};

    \draw[arrow] (pr) -- (actions);
    \draw[arrow] (actions) -- (helm);
    \draw[arrow] (helm) -- (dev);
    \draw[arrow] (dev) -- (prod);
    \draw[arrow] (prod.south) -- (cron.north);
    \draw[arrow] (cron.south) -- (logs.north);
\end{tikzpicture}
\end{document}
