\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.9cm, minimum height=0.55cm, align=center,
                line width=0.8pt, font=\scriptsize},
    group/.style={rectangle, rounded corners=5pt, draw=ugmblue, fill=ugmblue!5,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=1.8mm]}, line width=0.8pt, draw=gray!60},
    lbl/.style={font=\scriptsize\bfseries}
]

% Pipeline
\node[box=ugmblue] (pr) {PR deploy};
\node[box=ugmblue, below=0.4cm of pr] (gha) {GitHub Actions};
\node[box=ugmblue, below=0.4cm of gha] (helm) {Helm chart};
\node[group, fit=(pr)(gha)(helm), label={[lbl]above:CI/CD}] (ci) {};

% Environments
\node[box=degreen, right=2.2cm of gha] (dev) {dev env};
\node[box=degreen, below=0.4cm of dev] (prod) {prod env};

% CronJob
\node[box=dered, right=2.2cm of dev] (cron) {Kubernetes CronJob};
\node[box=dered, below=0.4cm of cron] (val) {validate job + logs};

\draw[arr] (pr.south) -- (gha.north);
\draw[arr] (gha.south) -- (helm.north);
\draw[arr] (helm.east) -- (dev.west);
\draw[arr] (dev.south) -- (prod.north);
\draw[arr] (dev.east) -- (cron.west);
\draw[arr] (cron.south) -- (val.north);

\end{tikzpicture}
\end{document}
