\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.8cm, minimum height=0.7cm, align=center,
                line width=0.9pt, font=\large},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=2mm]}, line width=0.9pt, draw=gray!60},
    lbl/.style={font=\large\bfseries}
]

% CI/CD top row
\node[box=ugmblue] (pr) {PR deploy};
\node[box=ugmblue, right=0.5cm of pr] (gha) {GitHub Actions};
\node[box=ugmblue, right=0.5cm of gha] (helm) {Helm chart};
\node[group=ugmblue, fit=(pr)(gha)(helm), label={[lbl]above:CI/CD}] (ci) {};

% Environments + CronJob (bottom row)
\node[box=degreen, below=1.0cm of gha] (dev) {dev env};
\node[box=degreen, right=0.5cm of dev] (prod) {prod env};
\node[box=dered, right=0.5cm of prod] (cron) {Kubernetes CronJob};
\node[box=dered, below=0.3cm of cron] (val) {validate job + logs};

\draw[arr] (pr.east) -- (gha.west);
\draw[arr] (gha.east) -- (helm.west);
\draw[arr] (gha.south) -- (dev.north);
\draw[arr] (helm.south) -- (dev.north);
\draw[arr] (dev.east) -- (prod.west);
\draw[arr] (prod.east) -- (cron.west);
\draw[arr] (cron.south) -- (val.north);

\end{tikzpicture}
\end{document}
