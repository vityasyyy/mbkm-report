\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.6cm and 0.55cm]
    \node[appbox] (source) {DE-WR\\Weekly Report writer};
    \node[processbox, right=of source] (ingest) {Ingest, normalize,\\and upsert reports/issues};
    \node[processbox, right=of ingest] (db) {Shared PostgreSQL\\organizations, employees,\\weekly\_reports, issues};
    \node[appbox, right=of db] (reader) {DE-WRI\\read and detect recurrence};
    \node[externalbox, right=of reader] (sidefx) {GitHub issue\\+ employee email};
    \node[groupbox=diagramgreen, fit=(source)(ingest),
        label={[grouplabel]above:Writer path}] (writer) {};
    \node[groupbox=diagramred, fit=(reader)(sidefx),
        label={[grouplabel]above:Reader + escalation}] (escalation) {};

    \draw[arrow] (source) -- (ingest);
    \draw[arrow] (ingest) -- (db);
    \draw[arrow] (db) -- (reader);
    \draw[arrow] (reader) -- (sidefx);
\end{tikzpicture}
\end{document}
