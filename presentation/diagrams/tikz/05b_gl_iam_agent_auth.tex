\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.65cm]
    \node[appbox] (human) {Human user\\session JWT};
    \node[processbox, right=of human] (issue) {Issue delegation\\token};
    \node[processbox, right=of issue] (token) {Scoped, expiring\\delegation token};
    \node[appbox, right=of token] (agent) {AI agent\\cross-service caller};
    \node[processbox, right=of agent] (validate) {Validate token\\scope + expiry};
    \node[processbox, below=0.75cm of validate, xshift=-1.6cm] (allow) {Authorize\\scope within parent};
    \node[externalbox, right=0.35cm of allow] (deny) {Deny\\revoked or expired};
    \node[groupbox=diagramgreen, fit=(issue)(token)(validate)(allow),
        label={[grouplabel]above:GL-IAM delegation}] (delegation) {};

    \draw[arrow] (human) -- (issue);
    \draw[arrow] (issue) -- (token);
    \draw[arrow] (token) -- (agent);
    \draw[arrow] (agent) -- (validate);
    \draw[arrow] (validate.south) -- (allow.north);
    \draw[arrow] (validate.south) -- (deny.north);
\end{tikzpicture}
\end{document}
