\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.9cm, minimum height=0.7cm, align=center,
                line width=0.9pt, font=\large},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=2mm]}, line width=0.9pt, draw=gray!60},
    lbl/.style={font=\large\bfseries}
]

% Top row: human -> token issue -> agent
\node[box=ugmblue] (human) {human};
\node[box=ugmblue, below=0.3cm of human] (usess) {user session JWT};
\node[group=ugmblue, fit=(human)(usess)] (usergrp) {};

\node[box=degreen, right=1.2cm of human] (issue) {create\_delegation\\token};
\node[box=degreen, below=0.3cm of issue] (jwt) {JWT delegation\\token};

\node[box=dered, right=1.2cm of issue] (agent) {AI agent};
\node[box=dered, below=0.3cm of agent] (validate) {validate\_delegation\\token stateless};

% Outcomes (bottom row)
\node[box=degreen, below=1.2cm of jwt] (authz) {authorize\\scopes $\subseteq$ parent};
\node[box=dered, below=0.3cm of authz] (deny) {deny\\revoked / expired};

\draw[arr] (usess.east) -- (issue.west);
\draw[arr] (issue.south) -- (jwt.north);
\draw[arr] (issue.east) -- (agent.west);
\draw[arr] (agent.south) -- (validate.north);
\draw[arr] (jwt.south) -- (authz.north);
\draw[arr] (validate.south) -- (deny.north);

\end{tikzpicture}
\end{document}
