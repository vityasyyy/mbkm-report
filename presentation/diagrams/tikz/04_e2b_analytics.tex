\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.7cm, minimum height=0.7cm, align=center,
                line width=0.9pt, font=\large},
    group/.style={rectangle, rounded corners=5pt, draw=#1, fill=#1!6,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=2mm]}, line width=0.9pt, draw=gray!60},
    lbl/.style={font=\large\bfseries}
]

% Input -> Agent
\node[box=dered] (in) {Meemo data};
\node[box=ugmblue, right=1.2cm of in] (a1) {analytics\_agent};
\node[box=ugmblue, below=0.3cm of a1] (a2) {e2b\_code\_interpreter tool};
\node[group=ugmblue, fit=(a1)(a2), label={[lbl]above:Agent}] (agn) {};

% Sandbox
\node[box=degreen, right=1.2cm of a1] (s1) {E2B sandbox\\(isolated Python)};
\node[box=degreen, below=0.3cm of s1] (s2) {Sandbox.run\_code()\\matplotlib + ReportLab};
\node[box=degreen, below=0.3cm of s2] (s3) {Big-4 MoM PDF};
\node[group=degreen, fit=(s1)(s2)(s3), label={[lbl]above:E2B sandbox}] (e2b) {};

% Output
\node[box=dered, right=1.2cm of s1] (out) {Email + Drive link};
\node[font=\large\itshape, below=0.4cm of out] (cron) {cron \textbullet{} Fri 17:00};

\draw[arr] (in.east) -- (a1.west);
\draw[arr] (a1.east) -- (s1.west);
\draw[arr] (a2.east) -- (s2.west);
\draw[arr] (s3.east) -- (out.west);

\end{tikzpicture}
\end{document}
