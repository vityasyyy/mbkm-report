\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.75cm]
    \node[externalbox] (input) {Meemo meeting data\\and project context};
    \node[appbox, right=of input] (agent) {analytics\_agent\\report orchestration};
    \node[processbox, right=of agent] (sandbox) {E2B sandbox\\isolated Python};
    \node[processbox, below=0.45cm of sandbox] (code) {run\_code()\\matplotlib + ReportLab};
    \node[groupbox=diagramgreen, fit=(sandbox)(code),
        label={[grouplabel]above:Execution boundary}] (runtime) {};
    \node[externalbox, right=of sandbox] (output) {MoM PDF\\email + Drive link};
    \node[appbox, below=0.45cm of output] (schedule) {scheduled weekly\\AIP execution};

    \draw[arrow] (input) -- (agent);
    \draw[arrow] (agent) -- (sandbox);
    \draw[arrow] (sandbox) -- (code);
    \draw[arrow] (code.east) -| (output.south);
    \draw[arrow] (schedule.west) -| (sandbox.south);
\end{tikzpicture}
\end{document}
