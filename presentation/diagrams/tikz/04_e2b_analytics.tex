\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}

\definecolor{ugmblue}{HTML}{194168}
\definecolor{degreen}{RGB}{76,175,80}
\definecolor{dered}{RGB}{229,115,115}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 0.5cm,
    box/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!8,
                minimum width=2.8cm, minimum height=0.58cm, align=center,
                line width=0.8pt, font=\scriptsize},
    group/.style={rectangle, rounded corners=5pt, draw=ugmblue, fill=ugmblue!5,
                  inner sep=6pt, line width=1.1pt},
    arr/.style={-{Latex[length=1.8mm]}, line width=0.8pt, draw=gray!60},
    lbl/.style={font=\scriptsize\bfseries}
]

% Agent
\node[box=ugmblue] (agent) {analytics\_agent};
\node[box=ugmblue, below=0.35cm of agent] (tool) {e2b\_code\_interpreter\_tool};
\node[group, fit=(agent)(tool), label={[lbl]above:Agent}] (agn) {};

% Sandbox
\node[box=degreen, right=2.2cm of agent] (sb) {E2B sandbox\\(isolated Python)};
\node[box=degreen, below=0.35cm of sb] (code) {Sandbox.run\_code()\\matplotlib + ReportLab};
\node[box=degreen, below=0.35cm of code] (pdf) {Big-4 MoM PDF};
\node[group=degreen, fit=(sb)(code)(pdf),
      label={[lbl]above:E2B sandbox}] (e2b) {};

% Inputs / outputs
\node[box=dered, left=1.6cm of agent, minimum width=2.2cm] (meemo) {Meemo data};
\node[box=dered, right=1.6cm of pdf, minimum width=2.6cm] (out) {Email + Drive link};

% Schedule
\node[font=\scriptsize\itshape, below=0.5cm of e2b] (cron) {cron \textbullet{} Fri 17:00};

\draw[arr] (meemo.east) -- (agent.west);
\draw[arr] (agent.east) -- (sb.west);
\draw[arr] (sb.south) -- (code.north);
\draw[arr] (code.south) -- (pdf.north);
\draw[arr] (pdf.east) -- (out.west);

\end{tikzpicture}
\end{document}
