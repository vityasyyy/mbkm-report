\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,backgrounds,calc,arrows.meta}
\usepackage{xcolor}
\input{style/diagram-style}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.8cm]
    \node[externalbox] (sources) {Google / Meemo\\GitHub / Drive};
    \node[processbox, right=of sources] (connectors) {GL Connectors SDK\\external-service facade};
    \node[groupbox=diagramred, fit=(sources)(connectors),
        label={[grouplabel]above:External services}] (external) {};

    \node[appbox, right=1.2cm of connectors] (runtime) {AIP runtime\\digital\_employee\_core};
    \node[groupbox=ugmblue, fit=(runtime),
        label={[grouplabel]above:Digital Employee}] (aip) {};

    \node[processbox, right=1.2cm of runtime] (iam) {GL-IAM SDK\\SIMI identity layer};
    \node[externalbox, below=0.45cm of iam] (cookbook) {GL-IAM Cookbook\\integration examples};
    \node[groupbox=diagramred, fit=(iam)(cookbook),
        label={[grouplabel]above:Shared identity}] (shared) {};

    \node[appbox, below=1.15cm of runtime, xshift=-2.6cm] (weekly) {Weekly Report\\Claudia};
    \node[appbox, right=0.35cm of weekly] (recur) {Issue Reoccurrence\\escalation + sync};
    \node[appbox, right=0.35cm of recur] (pamela) {Project Manager\\Pamela};
    \node[appbox, right=0.35cm of pamela] (datasaur) {PM Datasaur\\knowledge aggregation};
    \node[groupbox=ugmblue, fit=(weekly)(recur)(pamela)(datasaur),
        label={[grouplabel]below:Product families from de-main}] (products) {};

    \draw[arrow] (sources) -- (connectors);
    \draw[arrow] (connectors) -- (runtime);
    \draw[arrow] (runtime) -- (iam);
    \draw[arrow] (runtime.south) -- (weekly.north);
    \draw[arrow] (runtime.south) -- (recur.north);
    \draw[arrow] (runtime.south) -- (pamela.north);
    \draw[arrow] (runtime.south) -- (datasaur.north);
    \draw[arrow] (iam.south) -- (cookbook.north);
\end{tikzpicture}
\end{document}
